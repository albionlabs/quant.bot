app = 'quant-bot-tools'
primary_region = 'lhr'

[build]
  dockerfile = 'apps/tools/Dockerfile'

[env]
  TOOLS_HOST = '::'
  ORDERBOOK_API_URL = 'http://albion.internal:8000'

[[vm]]
  size = 'shared-cpu-1x'
  memory = '512mb'
